# CAMPUSGUARD — WINNING DEMO SCRIPT

## Target Duration: 3 Minutes

**1. Power drops by 30%**
- Trigger a simulated power failure in the backend.
- Infrastructure telemetry visually responds.

**2. Command Center detects institutional impact**
- Navigate to Command Center.
- Point out the active incident banner highlighting capacity shortfall.
- Show how the failure propagates down to active missions.

**3. Continuity Conflict appears**
- Navigate to Continuity Conflict.
- Show the visual breakdown: Infrastructure capacity cannot sustain the protected demand envelope.

**4. Change active obligation context**
- Select the `Exam + Research + Emergency` context.
- Switch to the `Emergency Only` context.

**5. Same failure produces different response**
- Point out how the selected recommendation natively changes despite the infrastructure failure remaining at exactly -30% power. "Context defines the optimal response."

**6. Explore Counterfactual**
- Navigate to Counterfactual Playground.
- Safely adjust parameters (e.g., student Wi-Fi).
- Point out that live state remains unmutated while projected outcomes shift dynamically.

**7. Tournament compares strategies**
- Navigate to Recovery Plan Tournament.
- Show the available candidate strategies (Do Nothing, Greedy Shedding, etc.).

**8. ICO selected**
- Highlight the ICO (Institutional Continuity Optimizer) winning strategy.
- Show its justification parsing cost minimization and constraint protection natively.

**9. Safety Gate requires human authorization**
- Proceed to Safety Gate.
- Note the "LESS EVIDENCE → LESS AUTONOMY" constraint checking active telemetry signatures.

**10. State changes**
- Quickly manually simulate a state change / or context switch in another tab to trigger invalidation constraints.

**11. Old approval becomes invalid**
- Point out the state fingerprint mismatch explicitly triggering a rejection. "APPROVAL INVALIDATED. STATE DRIFT DETECTED."

**12. Re-evaluate**
- Back track the pipeline to re-fetch the active state optimizations.

**13. New approval**
- Return to Safety Gate. Sign the form verifying the action parameters exactly matching current telemetry boundaries.

**14. Controlled simulated execution**
- Navigate to Controlled Execution.
- Perform a Dry Run.
- Hit Execute. Watch dispatch success logic safely trigger without bypassing limits.

**15. Verification**
- Navigate to Recovery Verification.
- Show explicit ledger checking predicted bounds vs actual active limits, ensuring SLA constraints were mathematically protected.

**16. Degraded telemetry**
- Navigate to Degraded Telemetry.
- Simulate an HVAC sensor loss.
- Observe how the high-confidence threshold drops to `LOW` natively blocking execution scopes autonomously.

**17. Replay / Provenance**
- Navigate to Replay.
- Show forensic chronology of the entire decision chain securely coupled to state-fingerprints.

**18. Benchmark**
- Show the Optimization Benchmark parsing deterministic fairness tests validating algorithm reliability.
